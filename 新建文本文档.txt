    CC_DEC_BUFFER_ELEM_S *pstDecBufElem = m_pDecBufferQ.getEmpty();
    if (NULL == pstDecBufElem)
    {
        OSA_WARNING ("EncBufQ no empty buffer \n");
    
        pstDecBufElem  = m_pDecBufferQ.getFull();
    
        if (NULL != pstDecBufElem)
        {
            m_pDecBufferQ.putEmptyOrgin(pstDecBufElem);
        }
    }
    else
    {
        if (NULL != pstDecBufElem->pstRgb4RK)
        {
            pstDecBufElem->pstRgb4RK->pu8VirAddr[0]
            memcpy(pstDecBufElem->pstRgb4RK->pu8VirAddr[0], pstDecBuf->dataAddr, read_size);
            RK_MPI_MB_SetSzie((MEDIA_BUFFER)pstDecBufElem->pstRgb4RK->pMediaBuf, read_size);
        }
        else
        {
            iRet = OSA_EFAIL;
            printf("[%s]pstDecBufElem->pstRgb4RK is null\n", __FUNCTION__);
        }
    
        if (iRet)
        {
            OSA_Printf ("[%s] Give RV1126 Encoder error \n", __FUNCTION__);
            m_pDecBufferQ.putEmptyOrgin( pstDecBufElem );
        }
        else
        {
            //VDO_CAM_GetECameraPicture( "/tmp/giveRV1126", pstEncBufElem->pstRgb4RK->pu8VirAddr[0], 
            //  pstEncBufElem->pstRgb4RK->u16Width * pstEncBufElem->pstRgb4RK->u16Height * 3 / 2);
            pstDecBufElem->uiCount      = m_u32Count;
            pstDecBufElem->u64TimeStamp = u64TimeStamp;
            m_pDecBufferQ.putFullEnd( pstDecBufElem );
        }
    }

工作描述：主要工作职责是负责公司产品中摄像头智能应用相关工作。入职以来主要负责两个项目功能的开发，1.M500机型SG功能的开发


1.M500 SG功能效果调优，优化提高框选稳定性和框选实时性。
2.RV1126芯片编译环境搭建，基础demo验证。
3.IWB RV1126芯片检测算法框架搭建，与SDM845的通信搭建。

1.M500 SG功能效果调优，优化提高化框选稳定性和框选实时性 。（2月）
2.RV1126芯片编译环境搭建，编写RGA裁剪缩放demo和解码demo验证（2月中）
3.IWB RV1126芯片检测算法框架搭建，与SDM845 udp通信搭建。（3月）
具体如下：
1）.SDM845对图片进行H264编码发送，RV1126接收码流进行解码。
2）.RV1126对图片进行躯干检测，回传检测结果给SDM845。

1.10月至12月，辅助开发跟踪功能，效果调优。
2.11月至12月，分析处理UVC86发送过来的画面，实现M500在主机端支持SG功能。

1.编写跟踪功能的部分代码，协助分析修改开发过程中引入的bug。
2.M500+单个UVC86的协同工作，开启SG后，UVC86回到idle全景状态，主机M500接收UVC86传来的4K全景画面，用于视频分析和显示，其处理流程与A20/A10等基本一致。

	
掌握AF/VT/多分屏MW/多流SG的基础处理流程，并输出总结文档
